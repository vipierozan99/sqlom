"""Profiler adapters (PLAN.md §10): cProfile, yappi, pyinstrument (in-process)
and py-spy, austin (external). All output normalises to speedscope JSON +
folded stacks (`render.py`), stored inside the run directory.
"""
