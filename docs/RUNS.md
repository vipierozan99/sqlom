# Recorded runs

Dated runs from `bench record` (PLAN.md D15). Each row's branch holds the
full `results/runs/<run_id>/` artifact — `results/runs/` itself is gitignored
on main, so this table is how a run is found later. `bench verify <run_id>`
reproduces the command that produced a row (not its numbers — PLAN.md D3, no
old-vs-new agreement gate). Nothing here is republished into
`docs/BENCHMARKS.md` (PLAN.md D5).

| date | run_id | suite | quotable | branch | note |
|---|---|---|---|---|---|
